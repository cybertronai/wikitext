# Build context for the Dockerfile — populated locally via
# `python fetch_data.py wikitext-103-raw-v1/`. ~543 MB of raw text;
# the canonical source is gs://wikitext-103-raw-v1.
wikitext-103-raw-v1/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
env/

# Native build artifacts
*.so

# Local symlinks to sibling repos (not portable, not for sharing)
.sutro-context

# Env / OS
.env
.DS_Store

# Local dev notes / scratch — explainers, plans, idea logs, experiment journals.
.scratch/

# Internal slot-claim metadata written by claim_slot.sh for cross-session
# coordination (session id + heartbeat). Not for upstream.
submissions/*/.CLAIMED
